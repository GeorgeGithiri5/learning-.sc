package OOP

object Operators {
  def main(args: Array[String]): Unit ={
    var a = 50;
    var b = 30;

    println("Addition of a + b = " + (a + b))
    println("Substraction of a - b = " + (a - b))
    println("Multplication of a*b = " + (a*b))
  }
}
